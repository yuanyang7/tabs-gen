# Core — installed via: pip install -e .
click>=8.1.0
numpy>=1.24.0
scipy>=1.11.0
soundfile>=0.12.0
librosa>=0.11.0
ffmpeg-python>=0.2.0
tqdm>=4.65.0
pretty_midi>=0.2.10
mido>=1.3.0
music21>=9.3.0
pydantic>=2.0.0
yt-dlp>=2024.1.0

# Stage 1: Demucs separation — pip install -e ".[separation]"
demucs>=4.0.0
torch>=2.0.0
torchaudio>=2.0.0

# Stage 1 (MDX backend): audio-separator — pip install -e ".[mdx]"
audio-separator[cpu]>=0.44.0

# Stage 2: Transcription — pip install -e ".[transcription]"
# Note: basic-pitch requires TensorFlow 2.x
basic-pitch>=0.3.0
crepe>=0.0.14

# Stage 2: Drum transcription — pip install -e ".[drums]"
# Note: madmom requires numpy<2; install after other deps
ADTLib>=1.0.0
madmom>=0.16.1

# Stage 4: Guitar Pro output — pip install -e ".[output]"
pyguitarpro>=0.10.0
